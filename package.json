{
  "name": "clutchd",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "benchmark": "turbo run benchmark --concurrency=1",
    "build": "turbo run build",
    "changeset": "changeset",
    "clean": "find . -type d \\( -name 'node_modules' -o -name '.turbo' -o -name 'dist' -o -name 'coverage' \\) -prune -print -exec rm -rf '{}' \\;",
    "dev": "turbo run dev",
    "preinstall": "npx only-allow pnpm",
    "format": "prettier --write \"**/*.{js,json,jsx,ts,tsx,md,mdx}\"",
    "postinstall": "manypkg check",
    "lint": "turbo run lint",
    "release": "turbo run build && changeset publish",
    "test": "turbo run test",
    "typesync": "npx typesync",
    "version-packages": "changeset version"
  },
  "dependencies": {
    "@changesets/cli": "2.26.1",
    "@clutchd/eslint-config-clutchd": "1.1.2",
    "@manypkg/cli": "0.21.0",
    "eslint": "8.43.0",
    "prettier": "2.8.8",
    "turbo": "1.10.6"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
