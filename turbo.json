{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["**/*.ts", "**/*.tsx"],
      "outputs": ["coverage/**"]
    }
  }
}
