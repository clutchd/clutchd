import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import Button from "./button";

<Meta title={"UI/Button/Backgrounds"} />

# Get started

With `MDX` we can define a story for `Button` right in the middle of our
markdown documentation.

<Canvas>
  <Story name="Default">
    <Button>Click Here</Button>
  </Story>
  <Story name="Primary">
    <Button background="primary">Click Here</Button>
  </Story>
  <Story name="Secondary">
    <Button background="secondary">Click Here</Button>
  </Story>
  <Story name="Accent">
    <Button background="accent">Click Here</Button>
  </Story>
  <Story name="Gradient">
    <Button background="gradient">Click Here</Button>
  </Story>
  <Story name="Glass">
    <Button background="glass">Click Here</Button>
  </Story>
</Canvas>
